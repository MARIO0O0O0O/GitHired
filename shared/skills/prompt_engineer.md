# SKILL: prompt_engineer
Capabilities:
  - Write deterministic, source-bounded Gemini CLI prompts.
  - Every prompt must: state the agent role, list hard rules, define exact output format, specify output file path, end with a STOP instruction and done signal.
  - Never write prompts that allow hallucination or inference beyond what is explicitly in source files.
