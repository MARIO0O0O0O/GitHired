# App 1 — ProfileDB
- What it does: Manages the portable PostgreSQL career database and professional inventory.
- Input it needs: Resume PDF, `folio_portable_schema.sql`, and session prompts.
- Output it produces: Populated Supabase tables and `master_professional_inventory.md`.
- Status: READY TO BUILD
